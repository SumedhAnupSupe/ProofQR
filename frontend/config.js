// Centralized frontend configuration.
//
// On Vercel this file is regenerated at build time from the
// PRESENCE_API_BASE_URL environment variable (see scripts/gen-config.js and
// vercel.json). For plain local development, edit this value directly.
window.PRESENCE_CONFIG = {
  API_BASE_URL: 'http://localhost:4000',
};
